# 多多短剧项目规划

> 本文档用于持续记录项目规划、关键决策、范围变化与后续行动，方便后续开发和回顾。

## 1. 当前状态

- 记录开始日期：2026-07-16
- 项目阶段：Agent Core A4c 已完成，多租户故事创作业务后端正在实施；团队邀请、成员管理与审计查询已完成，下一步进入故事项目与项目授权。
- 仓库状态：TypeScript + pnpm monorepo、Web/Server/Agent 工程和 `packages/ai` 运行时已建立；Agent Core A1–A4c 已通过验收，Server 已具备 HTTP、PostgreSQL、无密码 Web 身份、完整团队生命周期、租户上下文和租户审计基座。

## 2. 规划目标

通过逐步讨论，明确以下内容：

1. 项目定位与目标用户
2. 核心使用场景与功能范围
3. 首个可交付版本（MVP）
4. 技术方案与项目结构
5. 开发阶段、优先级和验收标准

## 3. 已确认事项

### 产品形态

- 产品核心是一个统一的 Agent 服务器。
- Agent 服务器承载主要业务逻辑、智能体能力和跨平台共享能力。
- 产品提供多个用户端平台：
  - Web
  - Android
  - iOS
  - HarmonyOS（鸿蒙）
- 各平台客户端连接同一个 Agent 服务，而不是分别实现相互独立的 Agent 系统。

当前可将整体产品形态概括为：**一个 Agent 服务核心，加多个跨平台混合客户端。**

### 业务覆盖范围

产品采用**短剧全流程平台**定位，业务范围覆盖以下三个阶段：

1. **内容创作**：创意、故事、剧本、角色、分镜和素材规划。
2. **项目生产**：项目立项、团队协作、拍摄制作、审核和交付。
3. **运营发行**：内容管理、平台发布、投放推广、数据分析和运营优化。

三个阶段不是相互独立的工具集合，而应形成统一业务闭环：

```text
创意与立项
    ↓
内容创作
    ↓
项目生产
    ↓
审核与交付
    ↓
运营发行
    ↓
数据反馈与优化
    └────────→ 返回创作或生产环节
```

Agent 服务器需要贯穿全流程，理解项目上下文，并协调不同阶段的任务、数据和人工确认。

### 首期业务切入方式

首期采用**全流程最小闭环**，不优先深挖某一个孤立阶段。目标是让一个短剧业务实例能够以最少但完整的能力依次经过：

1. 创意与立项
2. 内容创作
3. 项目生产
4. 审核与交付
5. 运营发行
6. 数据反馈与优化

首期设计原则：

- 每个阶段只实现支撑闭环所必需的能力。
- 各阶段共享统一的项目上下文和业务数据，避免通过人工复制在模块之间传递。
- Agent 能够识别当前阶段、下一步动作和需要人工确认的节点。
- 优先验证跨阶段流转和数据连通，再逐步增强单个阶段的专业深度。
- 首期完成标准以“一个短剧项目能够走完整个业务生命周期”为核心，而不是以某个模块功能数量为标准。

### 服务端与客户端的职责原则

采用**混合客户端**形态：

- Agent 服务器负责跨平台一致的核心能力，例如 Agent 编排、业务流程、会话状态、模型调用和通用工具调用。
- 客户端负责用户界面以及与设备相关的原生能力，例如录音、相机、文件访问、系统通知和本地缓存。
- 平台能力通过统一协议暴露给 Agent 服务器，尽量避免将通用业务规则分别复制到 Web、Android、iOS 和 HarmonyOS。
- 对离线能力、端侧模型和本地任务的支持暂不作为默认前提，后续根据实际场景决定。

### 各平台的产品角色

| 平台 | 产品定位 | 功能范围 |
| --- | --- | --- |
| Web | 完整工作台 | 提供完整的业务操作、Agent 使用、配置和管理能力 |
| Android | 精简移动端 | 保留适合移动场景的核心功能和设备原生能力 |
| iOS | 精简移动端 | 保留适合移动场景的核心功能和设备原生能力 |
| HarmonyOS | 精简移动端 | 保留适合移动场景的核心功能和鸿蒙平台能力 |

平台之间不追求界面和功能数量完全一致，而是共享账号、数据、会话和 Agent 能力。Web 负责复杂、完整的工作流；移动端强调便捷访问、轻量操作和原生设备能力。

### 移动端产品组成

移动端采用组合形态，由以下三部分构成：

1. **Agent 对话助手**：支持通过文字、语音、图片和文件发起任务或追问结果。
2. **任务与通知中心**：查看 Agent 执行状态、接收结果、处理异常，并审批需要用户确认的关键操作。
3. **精简业务功能**：提供适合移动场景的少量核心业务页面，不复制 Web 工作台中的全部复杂能力。

三部分共享同一套用户身份、会话上下文、任务状态和业务数据。

### 移动端首页与任务入口

- 移动端以 **Agent 对话**作为首页和默认主入口。
- 用户打开应用后，可以直接通过 Agent 发起新任务或继续对话。
- 如果存在正在进行中的任务，客户端应向用户弹出明确提示。
- 用户点击提示后，应直接进入对应的任务流程，避免再次查找或选择入口。
- 任务提示与跳转依赖服务端提供统一的任务状态和目标页面信息。
- 当只有一个进行中任务时，可以直接展示该任务的提示并一键进入流程。
- 当同时存在多个进行中任务时，显示统一的任务图标按钮：
  - 图标右上角使用数字角标展示进行中任务数量。
  - 点击图标后展开进行中任务列表。
  - 用户从列表中选择任务后进入对应流程。
- 多任务入口应保持可见，但不应阻断用户继续使用 Agent 对话。

### 移动端任务通知策略

当移动应用不在前台时，以下事件需要发送系统推送：

1. **任务完成**：通知用户任务结果已经可用。
2. **任务失败**：通知用户执行失败，并提供查看原因或重新处理的入口。
3. **等待用户操作**：任务需要用户确认、审批或补充信息时，及时通知用户。

用户点击系统推送后，应直接进入对应任务或待处理步骤。普通执行进度默认不发送系统推送，避免产生过多打扰；用户可在应用内查看实时进度。

### 移动端 MVP 范围原则

移动端首个版本同时交付以下三部分，不采用只做对话或延后业务页面的方案：

1. **Agent 对话助手**：支持用户发起任务、继续会话和查看 Agent 回复。
2. **任务与通知中心**：支持任务提示、任务列表、状态查看、用户确认、信息补充、结果查看和系统推送。
3. **精简业务功能**：首版即提供移动端核心业务页面，形成不依赖 Web 也能完成的关键移动场景。

“同时交付三部分”表示三类能力都要形成可用闭环，并不表示移动端需要复制 Web 的所有完整业务功能。具体业务页面清单仍需结合核心业务场景确定。

### 平台首发顺序

采用 **Web 先行、移动端后续** 的分阶段发布方式：

1. **第一阶段：Agent 服务器与 Web 工作台**
   - 优先建立 Agent 服务核心、账号和数据体系、任务模型及完整 Web 工作台。
   - 通过 Web 验证完整业务流程和 Agent 能力。
2. **第二阶段：移动端应用**
   - 在服务端接口和业务流程趋于稳定后，建设 Android、iOS 和 HarmonyOS 应用。
   - 每个移动端首版仍需包含 Agent 对话、任务与通知中心、精简业务功能三部分。

这一顺序降低多端同时开发造成的返工风险，但服务端协议在第一阶段就需要考虑后续移动端接入。Android、iOS 和 HarmonyOS 的具体发布顺序尚待确定。


### 核心业务方向

产品的核心业务是 **短剧内容创作**，围绕以下主流程构建业务能力：

```text
创意 → 故事 → 剧本 → 分镜 → 素材 → 成片
```

当前业务架构以“帮助用户完成一部短剧内容”为核心，而不是优先解决剧组协作、拍摄项目管理、内容发行或投放运营问题。

各阶段的初步职责如下：

| 阶段 | 核心产出 |
| --- | --- |
| 创意 | 题材、主题、受众、核心卖点和创作方向 |
| 故事 | 世界观、人物关系、主要冲突和故事大纲 |
| 剧本 | 分集内容、场景、对白和动作描述 |
| 分镜 | 镜头拆解、画面描述、镜头顺序和节奏 |
| 素材 | 角色、场景、图片、视频、配音、音乐等制作资源 |
| 成片 | 可预览、审核、修改和导出的短剧视频 |

上述阶段构成首要业务主线。

### 一级创作领域与项目关系

产品首先划分为两个相互独立的一级创作领域：

1. **故事创作**：围绕故事、剧情和剧本开展对话式创作。
2. **短剧创作**：围绕短剧从故事或剧本到成片的完整制作流程开展工作。

故事项目与短剧项目是两个独立的领域对象，具有各自的内容、状态、权限、任务和版本历史。故事项目可以推进为短剧项目，但“推进”不会修改原故事项目的类型，而是基于用户选定的故事或剧本版本创建一个新的短剧项目。

推进时采用**版本快照**规则：

- 短剧项目保存来源故事项目和来源版本信息，用于追溯。
- 故事项目的内容被复制为短剧项目自己的初始内容。
- 推进完成后，故事项目与短剧项目不再自动同步。
- 故事项目后续修改不会自动影响短剧项目。
- 短剧项目内的修改也不会回写故事项目。
- 一个故事项目可以根据不同版本或制作目标推进为多个短剧项目。
- 删除或归档来源故事项目不能破坏已经创建的短剧项目。

### 故事项目的当前边界

故事创作页面以纯对话框作为起始界面，并支持上传文件，包括上传多集剧本。故事项目主要服务于故事和剧本相关创作。

当前确认的最低领域结构为：

```text
故事项目
├── 多个创作对话
├── 上传文件
└── Agent 创作成果
    ├── 草稿成果
    └── 已确认成果
```

基本规则：

- 一个故事项目可以包含多个对话。
- 各对话保留独立消息历史，同时共享所属故事项目的上下文和已确认成果。
- 初始页面可以先显示空白对话，在用户首次发送消息或上传文件时自动创建故事项目。
- Agent 产生故事或剧本内容时，自动创建草稿成果。
- 用户确认草稿后，该成果成为项目的当前有效版本。
- 对话消息是交互记录，创作成果是独立业务数据；归档或删除对话不能删除已保存的成果。
- 上传文件需要保留原始文件、文件元数据、导入记录和处理状态。

以下细节暂缓到 Agent 和剧本模块的详细设计阶段：

- 上传剧本文件的深度解析方案。
- 剧本的内部结构化格式。
- 故事、人物、大纲和分集剧本等成果类型的具体拆分方式。
- 各类成果之间的依赖、版本和影响传播规则。

### 短剧项目的创作流程

短剧项目可以通过三种方式获得初始内容：

1. 从故事项目的指定版本创建快照。
2. 在短剧项目内直接导入单集或多集剧本。
3. 在短剧项目内从故事或剧本开始创作。

无论来源如何，短剧项目中的故事和剧本都是项目自己的可编辑内容。短剧项目拥有独立的剧本编辑流程和版本历史。

短剧项目的完整创作流程为：

```text
故事
  ↓
剧本
  ↓
分镜
  ↓
素材准备与管理
  ↓
分镜提示词创作与分镜合成
  ↓
视频剪辑与合成
  ↓
短剧预览与导出
```

流程不是要求所有阶段必须完整执行一次的刚性流水线：

- 直接导入剧本时，允许跳过故事阶段并从剧本阶段开始。
- 被跳过的故事阶段以后仍可补充，但不阻塞后续流程。
- 用户可以返回上游阶段修改内容。
- 上游变化不会自动删除或覆盖已有下游内容。
- 系统需要记录上下游内容的版本依赖，并将受影响的下游内容标记为待检查或需要重新生成。
- 用户可以决定继续沿用、局部更新或重新生成受影响内容。

### 短剧项目、剧集、场景与镜头

多集短剧采用“项目保存全局设定、剧集独立推进制作流程”的模型：

```text
短剧项目
├── 项目级故事、人物、风格和共享内容
├── 第 1 集：独立创作与制作状态
├── 第 2 集：独立创作与制作状态
└── 第 3 集：独立创作与制作状态
```

基本规则：

- 不同剧集可以处于不同创作阶段，并允许并行推进。
- 项目总体状态由各剧集状态聚合得出，不要求所有剧集同步前进。
- 独立短片继续使用统一模型，但系统内部只有一个剧集，界面可以隐藏剧集层级。
- 剧集内部采用固定的“场景—镜头”层级。
- 场景组织同一时间、地点或连续叙事段落中的剧本和制作内容。
- 镜头是提示词创作、素材引用、画面生成、视频生成和剪辑编排的基本单位。
- 场景和镜头使用稳定标识；重新排序不会改变其身份及已有引用。

当前核心内容层级为：

```text
短剧项目
└── 剧集
    └── 场景
        └── 镜头
```

### AI 与人工协作模式

采用 **混合协作模式**，同时支持自动化效率和人工精细控制：

1. **自动运行**
   - 用户描述创作目标后，Agent 可以连续推进创意、故事、剧本、分镜、素材和成片阶段。
   - 没有检查点或异常时，系统可以依次创建并执行各阶段任务，自动推进到最终结果。
2. **阶段检查点**
   - 用户可以在关键阶段设置确认点。
   - Agent 到达检查点后暂停，等待用户确认、修改或补充信息，再继续后续任务。
3. **人工编辑**
   - 用户可以随时暂停自动流程，进入对应阶段的专业编辑界面。
   - 编辑后的内容成为后续 Agent 任务的新输入，保持上下文和产物连续。
4. **恢复执行**
   - 人工修改或确认后，Agent 可以继续当前阶段任务，并在完成后推进后续阶段，而不是重新开始整个创作流程。

Agent 负责编排和推进流程，用户保留对关键创作产物的控制权。自动模式与人工模式共享同一套项目数据、阶段状态和版本记录。

### Agent 任务粒度

Agent 任务按创作阶段独立建模，不设置覆盖整个创作流程的“总任务”：

```text
创意任务 → 故事任务 → 剧本任务 → 分镜任务 → 素材任务 → 成片任务
```

基本规则：

- 每个创作阶段的执行都是一个独立 Agent 任务。
- 每个任务独立记录发起人、输入、状态、执行日志、检查点和输出产物。
- 阶段任务可以引用前序阶段的已确认产物，并产生供后续阶段使用的新产物。
- 单个任务可以独立暂停、恢复、失败重试或重新执行，不要求重启整个创作流程。
- 移动端和 Web 的任务中心展示这些阶段任务，而不是额外展示一个总任务。
- 跨阶段自动推进由流程编排关系负责，但不因此创建覆盖全部阶段的总任务实体。

### Agent Core 技术架构

Agent Core 采用“分层轻内核 + 可插拔能力端口”，由 Harness 层和 Agent 逻辑层组成：

- **Harness 层**负责模型—工具循环、事件、审批、取消、预算、checkpoint、恢复、副作用一致性，以及 Agent 运行所需的上下文、记忆、知识检索、运行存储和沙箱能力。
- **Agent 逻辑层**负责 Agent 定义、提示词、模型和工具策略、上下文/记忆/知识策略、完成条件和结构化输出。
- `@duoduo/ai` 继续负责 Provider 协议、认证和模型传输边界，Agent Core 不重复实现 Provider wire protocol。
- Session、Task、Run、Turn 是四个独立作用域：Session 隔离对话，Task 隔离业务目标和进度，Run 隔离一次执行尝试和版本，Turn 隔离一次模型决策和上下文快照。
- Harness 内只有 Execution Kernel 保持轻量；Context、Memory、Knowledge/RAG/Wiki、Artifact Runtime、运行存储、搜索和沙箱作为 Harness 内部独立能力模块或适配器，不能直接耦合进模型—工具循环。
- 租户、项目、用户身份和业务权限真相属于 Server 业务服务；Harness 只消费并强制执行明确的 `AuthorizationContext`，不复制业务授权规则。
- 剧本、图片、音频和视频等 Artifact 的领域语义、归属和业务状态属于业务服务；Harness 负责 `ArtifactRef`、执行血缘、大工具结果外置和模型上下文投影。
- Web、移动端、通知和面向产品的后台 Worker 属于业务服务与应用层，通过任务、事件和审批协议使用 Agent Core。
- 长期记忆采用 Hermes 风格的有界精炼记忆、完整 Session 档案和可选外部 Provider；模型只能提出持久记忆候选，不能直接改写全局事实。
- 知识检索采用结构化精确查询优先、全文与向量混合召回、可选 rerank 和强制引用；Wiki 是可审查的派生知识，Raw Source 和 Server 数据保持权威。
- Harness Runtime Facilities 首版采用 PostgreSQL、对象存储和队列等通用设施，通过内部端口保留替换空间；不提前引入重型 Agent Runtime、知识图数据库或多个外部记忆平台。
- 实施顺序是可靠执行内核、上下文与执行安全、记忆与知识，最后才是多 Agent 和高级检索。

详细模块边界、端口、实施阶段和验收标准见 [Agent Core 功能调研与生产架构方案](docs/architecture/agent-core-market-research.md)。

### 创作流程实例

设置独立的 **创作流程实例**，用于串联各阶段任务，但流程实例本身不是 Agent 任务。

```text
创作流程实例
├── 创意阶段 → 创意任务
├── 故事阶段 → 故事任务
├── 剧本阶段 → 剧本任务
├── 分镜阶段 → 分镜任务
├── 素材阶段 → 素材任务
└── 成片阶段 → 成片任务
```

基本职责：

- 保存流程适用的项目及内容范围。
- 保存阶段顺序、依赖关系、自动推进规则和检查点配置。
- 记录当前所处阶段以及各阶段关联的 Agent 任务。
- 当前阶段完成且满足推进条件后，创建并启动下一阶段任务。
- 某阶段失败、暂停或等待确认时，阻止依赖它的后续阶段继续执行。
- 流程实例负责业务编排和进度聚合；阶段任务负责具体执行、日志和产物生成。

因此，“创作流程实例”与“Agent 任务”是编排与执行的关系，不形成总任务与子任务关系。

### 短剧项目类型与内容粒度

系统同时支持两种短剧项目类型：

1. **独立短片**
   - 一个项目产出一部独立短剧成片。
   - 具有完整的创意、故事、剧本、分镜、素材和成片流程。
2. **连续剧集**
   - 一个项目包含多集内容。
   - 项目层保存整部剧共享的创作设定，各集分别保存本集的剧本、分镜、素材和成片。

初步领域层级为：

```text
短剧项目
├── 独立短片
└── 连续剧集
    ├── 第 1 集
    ├── 第 2 集
    └── 第 N 集
```

独立短片和连续剧集复用同一套创作流程、Agent 能力和产物模型。

### 统一内容层级

内部统一采用以下内容层级：

```text
短剧项目
└── 剧集
    └── 场景
        └── 镜头
```

具体规则：

- **连续剧集**：一个短剧项目包含多个剧集。
- **独立短片**：系统自动创建一个默认剧集，内部仍使用完整层级。
- 独立短片的默认剧集层可以在用户界面中隐藏，避免增加不必要的操作复杂度。
- 项目级数据保存整部作品共享的创意方向、世界观、人物和风格等信息。
- 剧集级数据保存本集故事、剧本、场景、分镜和成片等内容。
- 场景用于组织同一时间、地点或叙事段落中的内容。
- 镜头是画面生成、视频生成和剪辑编排的基本单位。

通过统一层级，Agent 工作流、状态流转、版本管理和编辑器可以同时服务独立短片与连续剧集。

### 团队空间与项目可见范围

用户在团队空间中开展创作，但项目支持两种可见范围：

```text
创作团队
├── 团队项目（默认）
│   └── 团队成员可见
└── 私人项目
    └── 仅指定个人可见
```

基本规则：

- 新项目默认归属于当前团队，并采用团队可见范围。
- 用户可以将某个项目改为仅个人可见。
- 项目仍处于团队工作空间和数据边界内，但私人项目需要记录明确的个人所有者。
- 团队项目可以支持成员共同创作；私人项目不向普通团队成员展示。
- 项目范围变化需要记录操作者、变化前后的范围和操作时间。
- 私人项目对普通成员隐藏，但团队管理员可以查看、编辑和管理。
- 管理员访问或修改私人项目时，应保留审计记录。

### 简化权限模型

系统权限角色仅包含两类：

1. **管理员**
   - 管理团队及团队成员。
   - 管理团队级配置和权限。
   - 对团队项目和私人项目均具有查看、编辑和管理能力。
2. **成员**
   - 使用团队提供的创作能力。
   - 创建和参与项目。
   - 只能在自身权限和项目可见范围内访问内容。

编剧、分镜师、素材制作、剪辑师、审核人等属于项目中的业务分工或成员标签，不作为系统级权限角色。

### 团队项目的协作权限

团队项目采用“团队可见、协作者可编辑”的规则：

- 团队中的所有成员都可以查看团队项目。
- 项目创建者默认拥有编辑权限。
- 成员被邀请为某个项目的协作成员后，可以编辑该项目。
- 未被邀请参与项目的普通成员只有查看权限，不能修改项目内容或发起会改变项目数据的 Agent 任务。
- 协作成员是项目与成员之间的关系，不是新的系统级角色。
- 团队管理员仍然拥有所有团队项目的查看、编辑和管理权限。
- 只有团队管理员和项目创建者可以邀请或移除项目协作成员。
- 普通协作成员不能继续邀请或移除其他协作成员，避免编辑权限无控制扩散。
- 成员被移出项目协作列表后，权限降为只读，但其历史操作记录继续保留。

权限关系可以概括为：

| 用户身份 | 团队项目权限 |
| --- | --- |
| 管理员 | 查看、编辑、管理 |
| 项目创建者 | 查看、编辑 |
| 项目协作成员 | 查看、编辑 |
| 其他普通成员 | 仅查看 |

### 混合素材模型

素材采用“项目素材库 + 镜头专属产物”的混合模型：

```text
短剧项目
├── 项目素材库
│   ├── 人物素材
│   ├── 场景素材
│   ├── 道具素材
│   ├── 图片与视频素材
│   ├── 配音、音乐与音效
│   └── 其他可复用素材
└── 剧集
    └── 场景
        └── 镜头
            ├── 引用项目素材
            └── 镜头专属生成结果
```

具体规则：

- 人物形象、场景、道具、音乐等跨镜头使用的内容进入项目素材库。
- 镜头通过引用关系使用项目素材，不为每个镜头重复复制公共素材。
- 为特定镜头生成的图片、视频、配音或其他结果直接关联该镜头。
- 镜头专属生成结果经过确认后，可以加入项目素材库，供其他镜头继续复用。
- 素材需要保留来源、生成参数、关联对象和版本信息，以支持 Agent 追踪上下文及重新生成。

### 素材版本与引用传播

共享素材采用 **版本化、显式更新** 规则：

- 修改公共素材时创建新版本，不直接覆盖旧版本。
- 镜头引用固定到明确的素材版本，保证已有生成结果可以复现。
- 新版本创建后，系统列出仍在使用旧版本的剧集、场景和镜头。
- 用户可以选择保持旧版本、全部升级，或仅升级部分引用对象。
- 更新引用后，相关镜头及其下游产物应标记为需要检查或重新生成，避免静默改变已有成片。
- Agent 可以分析版本变化的影响范围，并创建相应的更新任务，但不能在未经允许时直接覆盖用户已经确认的成果。

示例：

```text
人物素材 V1
├── 镜头 1
└── 镜头 2

创建人物素材 V2
└── 影响分析
    ├── 镜头 1：保持 V1
    └── 镜头 2：升级到 V2，并标记相关画面待更新
```


## 4. 讨论记录

### 2026-07-16：开始项目规划

- 决定使用 Markdown 持续记录规划过程。
- 规划文档保存在仓库根目录，避免在项目结构尚未确定时提前增加顶层目录。

### 2026-07-16：确认基础产品形态

- 产品以 Agent 服务器为核心。
- 首期需要考虑 Web、Android、iOS 和 HarmonyOS 四个平台。
- 四个平台共享同一个服务端能力，具体客户端职责仍需进一步确认。

### 2026-07-16：选择混合客户端形态

- 通用 Agent 能力和核心业务逻辑集中在服务器。
- 录音、相机、通知、文件和缓存等平台相关能力由客户端实现。
- 服务端与客户端之间需要设计统一的通信和能力调用协议。

### 2026-07-16：确认各平台产品角色

- Web 定位为功能完整的工作台。
- Android、iOS 和 HarmonyOS 定位为相对精简的移动应用。
- 移动端不要求复制 Web 的全部复杂功能，应围绕移动场景选择核心能力。
- 所有平台共享服务端账号、数据、会话和 Agent 能力。

### 2026-07-16：增加 HarmonyOS 平台

- 客户端平台增加 HarmonyOS（鸿蒙）。
- Web 继续作为完整工作台；Android、iOS 和 HarmonyOS 均作为相对精简的移动应用。
- HarmonyOS 与其他平台共享同一个 Agent 服务器、账号体系、数据和会话状态。

### 2026-07-16：确认移动端组合形态

- 移动端同时包含 Agent 对话、任务与通知、精简业务功能。
- 对话用于发起和追踪任务；任务中心用于查看状态、接收结果和审批操作；业务页面用于高频、明确的移动端操作。
- 首个版本不必一次实现三个部分的完整能力，需要继续确定入口和优先级。

### 2026-07-16：确认移动端首页和任务跳转

- 移动端默认首页为 Agent 对话。
- 存在进行中任务时，移动端向用户弹出任务提示。
- 用户点击一次即可进入对应任务流程。
- 移动端导航需要支持由任务标识直接定位到相应流程。

### 2026-07-16：确认多任务入口

- 同时存在多个进行中任务时，不逐个弹出任务提示。
- Agent 首页显示一个统一的任务图标按钮，并在图标右上角展示任务数量角标。
- 点击任务图标后展开任务列表，再由用户选择要进入的任务流程。

### 2026-07-16：确认移动端系统通知策略

- 任务完成、任务失败、等待用户确认或补充信息时发送系统推送。
- 点击推送后直接进入对应任务或待处理步骤。
- 普通执行进度不默认发送系统推送，避免频繁打扰。

### 2026-07-16：确认移动端 MVP 覆盖范围

- 移动端首版同时包含 Agent 对话、任务与通知中心、精简业务功能。
- 三部分在首版均需达到可用状态，不能只交付 Agent 对话或任务能力。
- 精简业务功能以关键移动场景为边界，不复制 Web 的全部功能。

### 2026-07-16：确认平台首发顺序

- 第一阶段优先上线 Agent 服务器和 Web 完整工作台。
- Web 用于验证 Agent 能力、任务模型和完整业务流程。
- Android、iOS 和 HarmonyOS 在服务端与 Web 相对稳定后进入后续阶段。
- 第一阶段的服务端设计仍需预留统一的移动端接入能力，避免后续重构核心协议。

### 2026-07-16：暂停跨平台规划，转入业务架构

- 当前不继续讨论 Android、iOS 和 HarmonyOS 的发布顺序及具体实现方案。
- 已确认的多平台总体方向保留，未确认的跨平台细节暂缓。
- 接下来优先明确核心业务、参与角色、业务对象、业务流程和 Agent 在业务中的职责。

### 2026-07-16：确认核心业务方向

- 产品首先聚焦短剧内容创作。
- 核心业务主线为：创意、故事、剧本、分镜、素材、成片。
- 剧组协作、传统拍摄管理、发行和投放运营暂不作为当前业务架构的中心。

### 2026-07-16：确认 AI 与人工混合协作模式

- Agent 默认可以自动推进完整创作流程。
- 用户可以设置阶段检查点，在关键产物生成后确认再继续。
- 用户可以随时暂停 Agent，进入专业界面进行人工编辑。
- 人工修改完成后，Agent 从当前阶段继续执行，并继承修改后的上下文。
- 自动和人工操作共享项目状态、业务数据和版本记录。

### 2026-07-16：确认 Agent 任务粒度

- 不设置覆盖创意到成片全过程的总任务。
- 创意、故事、剧本、分镜、素材和成片分别形成独立的阶段任务。
- 各阶段任务独立保存状态、日志、检查点和输出，并可单独暂停、恢复或重试。
- 跨阶段连续运行通过流程编排关系实现，而不是通过总任务实体实现。

### 2026-07-16：确认创作流程实例

- 设置创作流程实例，用于管理阶段顺序、依赖关系、检查点和自动推进规则。
- 流程实例串联创意、故事、剧本、分镜、素材和成片阶段任务。
- 流程实例不是 Agent 总任务，不负责具体执行和产物生成。
- 阶段任务负责执行；流程实例负责业务编排和整体进度聚合。

### 2026-07-16：确认短剧项目类型

- 系统同时支持独立短片和连续剧集。
- 独立短片在一个项目中完成一部成片。
- 连续剧集项目包含多集，并共享项目级创作设定和上下文。
- 两类项目应复用创作流程和 Agent 能力，避免形成两套完全独立的业务系统。

### 2026-07-16：确认统一内容层级

- 独立短片和连续剧集统一使用“项目、剧集、场景、镜头”四级结构。
- 独立短片在内部自动创建一个默认剧集，界面可以隐藏该层级。
- 连续剧集在一个项目下包含多个剧集。
- 统一模型用于复用 Agent 工作流、编辑器、状态管理和版本能力。

### 2026-07-16：确认混合素材模型

- 项目设置统一素材库，保存人物、场景、道具、图片、视频和音频等可复用内容。
- 镜头可以引用项目素材，同时保存自身专属的生成结果。
- 镜头结果确认后可以提升为项目公共素材。
- 素材保留来源、参数、关联对象和版本信息。

### 2026-07-16：确认素材版本和更新传播规则

- 公共素材修改时创建新版本，不覆盖旧版本。
- 已有镜头继续固定引用原素材版本。
- 系统展示新版本影响的剧集、场景和镜头。
- 用户可以选择不升级、全部升级或部分升级。
- 升级引用后，受影响的下游产物进入待检查或待重新生成状态。

### 2026-07-16：团队创作范围草案（已修订）

- 曾计划所有短剧项目均由整个创作团队共同访问。
- 该规则随后修订为：团队可见是默认范围，但项目可以设置为仅个人可见。
- Agent 的任务和修改仍需记录实际发起人、确认人及操作历史。

### 2026-07-16：修订项目范围与权限模型

- 新项目默认采用团队可见范围。
- 允许将指定项目设置为仅个人可见。
- 系统级权限角色简化为管理员和成员两类。
- 编剧、分镜、素材、剪辑和审核等仅作为业务分工，不作为系统权限角色。

### 2026-07-16：确认管理员可以访问私人项目

- 私人项目只对其他普通成员隐藏，不对团队管理员隐藏。
- 管理员可以查看、编辑和管理团队空间中的私人项目。
- 管理员对私人项目的访问和修改需要记录审计日志。

### 2026-07-16：确认团队项目协作权限

- 所有团队成员都可以查看团队项目。
- 项目创建者和受邀协作成员可以编辑项目。
- 未加入项目协作列表的普通成员只能查看。
- 团队管理员对所有团队项目保持查看、编辑和管理权限。
- 协作关系属于项目级授权，不增加新的系统角色。

### 2026-07-16：确认协作成员管理权限

- 团队管理员和项目创建者可以邀请或移除项目协作成员。
- 普通协作成员不能邀请或移除其他协作成员。
- 该规则用于避免项目编辑权限在协作者之间无控制扩散。

### 2026-07-16：划分故事创作与短剧创作

- 产品划分为故事创作和短剧创作两个一级领域。
- 故事项目与短剧项目完全独立。
- 故事项目可以基于指定成果版本创建新的短剧项目。
- 短剧项目接收的是故事项目成果的快照，推进后双方不自动同步。
- 短剧项目也可以直接导入剧本，并在项目内部继续编辑剧本。

### 2026-07-16：确认故事项目的对话模型

- 故事创作以纯对话框作为起始页面，并支持上传多集剧本等文件。
- 一个故事项目可以包含多个对话。
- 各对话保留独立历史，同时共享项目上下文和已确认成果。
- Agent 生成内容时自动建立草稿成果，用户确认后成为有效成果。
- 文件深度解析、成果类型拆分和内部结构暂缓到 Agent 实现阶段讨论。

### 2026-07-16：确认短剧创作流程和内容层级

- 短剧流程为故事、剧本、分镜、素材准备与管理、分镜提示词创作与分镜合成、视频剪辑与合成、短剧预览与导出。
- 导入剧本时允许跳过故事阶段。
- 允许返回上游阶段修改内容，受影响的下游内容进入待检查或待重新生成状态。
- 项目保存全局设定，各剧集独立并行推进制作流程。
- 短剧内容采用项目、剧集、场景和镜头的固定层级。

### 2026-07-16：全流程扩展草案（当前暂缓）

- 曾考虑覆盖短剧内容创作、项目生产和运营发行三个阶段。
- 三个阶段需要共享项目数据和上下文，形成从创作到发行、再由数据反馈驱动优化的闭环。
- Agent 服务器贯穿整个业务生命周期，而不是只服务某一个孤立环节。
- 该方向可作为远期扩展参考；当前已明确聚焦短剧内容创作，不作为现阶段业务边界。

### 2026-07-16：全流程首期草案（已被当前聚焦策略取代）

- 早期曾考虑不单独选择创作、生产或发行作为唯一重点。
- 首期为创作、生产、审核、发行和反馈分别提供最小必要能力。
- 以一个短剧项目能够走完整个生命周期作为首期业务验证目标。
- 该首期策略已被“优先完成短剧内容创作闭环”的当前决定取代。

### 2026-08-01：确认 Agent Core 生产架构

- 确认 Agent Core 由 Harness 层和 Agent 逻辑层组成，并使用分层轻内核与可插拔能力端口。
- 确认 Session、Task、Run、Turn 四级隔离，补充 ToolExecution 副作用账本和耐久提交边界。
- 确认集中式上下文装配、Hermes 风格分层记忆、Hybrid RAG、派生 Wiki、独立 Artifact 和沙箱隔离进入正式规划。
- 确认 Provider 归 `@duoduo/ai`，业务权限真相和客户端归业务层；运行存储、知识检索和沙箱归 Harness；Artifact 由业务层与 Harness 分工协作。
- 确认优先完成单 Agent 的一致性、恢复、安全和多租户隔离，再建设多 Agent 与高级知识能力。
- 详细设计与一手资料记录在 Agent Core 架构文档中。

### 2026-08-09：确认多租户故事创作后端框架

- 首个业务后端闭环聚焦故事项目、创作对话、草稿成果和确认版本；短剧制作链路后续再接入。
- 采用 NestJS 模块化单体与共享 PostgreSQL 业务库，不提前拆分微服务。
- 团队空间就是租户；用户可加入多个团队，所有业务数据通过租户上下文、应用授权和数据库复合约束隔离。
- 首期采用邮箱验证码或魔法链接的无密码登录，支持创建团队和通过邀请加入团队。
- 真实 Agent 调用首期暂缓；Server 定义 `AgentGateway` 与授权协议，由模拟适配器生成可预测草稿。
- 真实 Agent、队列、Redis、对象存储、文件处理和短剧制作模块按实际业务开发继续补齐。
- 详细模块边界、数据模型、API 约定、安全基线、测试策略和分阶段验收记录在[多租户故事创作后端框架设计](docs/superpowers/specs/2026-08-09-multi-tenant-story-backend-framework-design.md)中。

## 5. 当前待讨论事项

### 业务架构

- 核心领域对象
- 主要业务流程
- Agent 在各业务环节中的职责
- 业务模块边界
- 权限、审批和状态流转
- 业务 MVP 范围

### 暂缓事项

以下内容保留已有结论，但暂不继续展开：

- 移动端 MVP 的精简业务页面清单
- Android、iOS 和 HarmonyOS 的发布顺序
- 服务端调用客户端平台能力的具体协议
- 跨平台技术栈与实现方式
- 剧组生产管理与运营发行等全流程扩展

## 6. 行动项

- [ ] 明确项目定位和目标用户
- [x] 确认基础产品形态
- [x] 明确 Agent 服务器与各客户端的基本职责边界
- [x] 明确 Web、Android、iOS 和 HarmonyOS 的具体产品角色
- [x] 确定移动端的核心产品组成
- [x] 确定移动端首页与任务入口
- [x] 确定多个进行中任务的提示和选择规则
- [x] 确定移动端任务通知策略
- [x] 确定移动端三个组成部分的 MVP 覆盖范围
- [ ] 确定移动端 MVP 的精简业务页面清单（暂缓）
- [x] 确定 Web 与移动端的首发顺序
- [ ] 确定 Android、iOS 和 HarmonyOS 的发布顺序（暂缓）
- [ ] 设计服务端与客户端的平台能力调用协议（暂缓）
- [ ] 确定 MVP 边界
- [x] 明确核心业务目标与业务闭环
- [x] 确定 AI 与人工在内容生产中的协作方式
- [x] 确定 Agent 任务的业务粒度
- [x] 确定独立阶段任务的流程串联方式
- [x] 确定短剧项目的内容类型和基本粒度
- [x] 确定独立短片与连续剧集的统一内部模型
- [x] 确定项目、剧集、场景和镜头的基本层级
- [x] 确定素材的归属、复用和引用模型
- [x] 确定共享素材的版本和更新传播规则
- [x] 确定团队项目与私人项目的范围模型
- [x] 确定管理员与成员两级角色模型
- [x] 确定私人项目对管理员的可见性
- [x] 确定管理员与成员对团队项目的基本操作权限
- [x] 确定项目协作成员的邀请和移除权限
- [x] 划分故事创作与短剧创作两个一级领域
- [x] 确定故事项目推进为短剧项目的快照规则
- [x] 确定故事项目与创作对话的基本关系
- [x] 确定短剧项目的核心创作流程
- [x] 确定短剧项目按剧集独立推进的流程粒度
- [x] 确定短剧项目、剧集、场景和镜头的内容层级
- [x] 确定上游变化对下游产物的基本影响规则
- [x] 调研主流 Agent SDK、Runtime 和 Harness 的生产功能
- [x] 确认 Agent Core 的四级隔离、上下文、分层记忆、RAG/Wiki、Artifact、沙箱与可靠执行架构
- [ ] 按 Agent Core 架构阶段 A 实现可靠执行内核
  - [x] A1：无状态 Executor、内存 Harness、Task/Run/Turn 作用域与并发隔离
  - [x] A2：耐久状态、事件/outbox、cursor/replay 与 checkpoint 可恢复边界
  - [x] A3：ToolExecutionLedger、Attempt、幂等键、副作用分类、deadline 与 `unknown` 语义
  - [x] A4：持久化审批、恢复扫描、外部对账与安全自动恢复
    - [x] A4a：持久化审批、跨实例决定与 exactly-once 消费
    - [x] A4b：Run 租约/fencing、恢复 Worker、PostgreSQL 竞争与进程重启恢复
    - [x] A4c：外部副作用对账与人工处置闭环
- [ ] 研究全流程扩展（暂缓）
- [ ] 确定核心领域对象与层级
- [ ] 明确业务角色、领域对象和主要流程
- [x] 划分首个业务后端的模块和 Agent 职责
- [x] 选择首个业务后端的技术方案
- [x] 制定首个业务后端的实施阶段和验收标准
