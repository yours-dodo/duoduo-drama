# 多多短剧领域词汇

本词汇表统一多多短剧业务中的核心概念。产品、接口、领域模型和文档使用同一组术语，避免身份与租户边界产生歧义。

## 身份与租户

**用户（User）**：
跨团队存在的全局个人身份，同一用户可以加入多个团队。
_避免：账号、租户用户_

**团队（Team）**：
用户协作和业务数据隔离的租户空间；团队与租户是同一个业务概念。
_避免：组织、工作区、企业_

**团队成员关系（Team Membership）**：
用户加入某个团队后形成的归属关系，并承载其当前团队角色和有效状态。
_避免：团队用户、租户账号_

**团队管理员（Team Admin）**：
可以管理团队成员和邀请的团队成员；每个团队必须始终保留至少一名有效管理员。
_避免：所有者、超级管理员_

**团队成员（Team Member）**：
不具备团队管理权的普通团队成员。
_避免：普通用户、访客_

**团队邀请（Team Invitation）**：
邀请一个指定邮箱对应的用户加入单个团队的限时凭证；邀请本身不授予团队访问权。
_避免：成员资格、邀请码_

**租户上下文（Tenant Context）**：
一次请求中由目标团队和有效成员关系共同确定的不可变工作范围。
_避免：当前团队、会话团队_

## 故事项目与授权

**故事项目（Story Project）**：
团队内承载故事、剧情和剧本创作的业务对象，拥有独立的生命周期和并发修订号。
_避免：项目、故事工作区_

**项目可见范围（Project Visibility）**：
故事项目在团队范围内的可见边界；团队项目对全部团队成员可见，私人项目只对创建者和团队管理员可见。
_避免：项目权限、项目类型_

**项目协作者（Project Collaborator）**：
被授予团队项目编辑权限的团队成员；该关系不改变成员的团队角色，项目私人化后不产生访问权。
_避免：项目管理员、项目角色_

**项目修订号（Project Revision）**：
故事项目每次可编辑变更递增的版本号，调用方必须用它声明预期状态，以避免覆盖并发修改。
_避免：项目版本、内容版本_

**创作对话（Conversation）**：
故事项目内独立保存消息历史的创作上下文；一个故事项目可以包含多个对话，对话可以单独归档。
_避免：项目会话、聊天窗口_

**对话消息（Message）**：
创作对话中的不可变追加记录，记录用户、Agent 或系统的发言；消息历史不等同于故事成果。
_避免：对话内容、成果版本_

**故事生成请求（Story Generation Request）**：
由用户消息触发、记录输入快照和处理状态的生成意图；它与消息分开建模，以便后续安全重试和接入 Agent。
_避免：消息任务、Agent 消息_
