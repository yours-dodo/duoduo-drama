# 移除世界构成实体搜索

## 目标

从世界观工作区的“世界构成”视图移除实体搜索能力，让左侧实体区域直接展示全部世界要素。

## 设计

- 删除“搜索实体”输入框及其图标。
- 删除搜索关键词状态与实体筛选计算逻辑。
- 实体列表直接遍历全部实体数据。
- 删除仅用于搜索无结果场景的提示。
- 删除不再使用的搜索框样式。
- 保留实体计数、实体选择、选中状态和右侧关系展示行为。

## 验证

- 页面不再显示“搜索实体”输入框。
- 世界构成中的全部实体仍可见并可选择。
- 选择实体后，右侧详情仍正常更新。
- 运行 Web 类型检查并检查代码差异。
